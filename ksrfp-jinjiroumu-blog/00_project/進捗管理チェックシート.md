# ksrfp-jinjiroumu-blog 進捗管理チェックシート

## 0. プロジェクト基本情報

- [x] プロジェクト名を確認する: `ksrfp-jinjiroumu-blog`
- [x] 作業フォルダを確認する: `/Users/ug/Desktop/codex_works/ksrfp-jinjiroumu-blog`
- [x] 対象サイトを確認する: https://ksrfp.com/
- [x] 対象ブログを確認する: https://ksrfp.com/blog/
- [x] 事務所情報を確認する
- [x] WordPressアプリケーションパスワード情報の保管場所を確認する

## 現在の進捗

- 更新日時: 2026-06-21 13:29
- 成功状態の保存完了: 成功ランの実物生成物・ログを含むローカルバックアップ `00_project/backups/ksrfp-success-state-20260621-1329.tar.gz` を作成した。秘密情報、ブラウザプロファイル、Pythonキャッシュ、OSファイル、バックアップ自身は除外済み。
- Git基準点作成完了: コード、設定、仕様書、入力データ、README、空ディレクトリ用ファイルをコミット `daf33d0`（`Add ksrfp blog automation baseline`）として保存した。秘密情報、生成ログ、生成物、状態ファイル、バックアップアーカイブはGit管理対象外。
- 追加課金画像生成除外後の軽量回帰テスト完了: `compileall`、JSON構文チェック、禁止語検索、画像生成プリフライト、画像生成要求時の停止挙動を確認した。Python側では画像ファイルを生成せず、`codex_image_tool_handoff` / `handoff_required=true` としてCodex画像生成ツールへ引き継ぐ。
- 追加課金画像生成の除外: 追加契約や従量課金につながる画像生成の環境変数、秘密ファイル、呼び出し実装をコード・設定・ドキュメント・既存生成JSONから削除した。画像生成はChatGPT/Codex側の利用中プラン内で使える画像生成ツールへ引き継ぐ運用に統一した。
- ユーザー最終レビュー完了: WordPress下書きとGoogle Drive確認用テキストをユーザーが確認済み。アイキャッチ画像、記事構成、内容、文章、見出しはいずれも運用品質としてOK判定。
- 本番相当フルテスト完了: `run_key=2026-06-21T13:03:42` は `status=ok` で完了。対象号は `人事労務だより2026.5月号.pdf`、生成記事数は3件、`drive_status=ok`、`wordpress_status=ok`。
- WordPress下書き保存完了: 投稿ID `4807`、`4809`、`4811` を作成。3件とも `draft`、投稿日 `2026-06-22T09:00:00+09:00`、投稿者ID2、タグなし、スラッグなし、Arkhe CSS保存、H1重複なし、H2/H3あり、禁止表現なしを読み返し検証で確認した。
- Google Drive確認用テキスト保存完了: 指定フォルダ `1vFh7U9F2DhItpYRzVvJ2qRKUpvmOD7jp` に3件保存し、DriveファイルIDは `1eftPEtrqYNUn4pJBBVlUcDbIsYSwwtr8`、`1l22CL12KYJgSLJFghDjK-7JAxvOD_X_4`、`12S5UtHY0OIEg8l5aTq-I0dM5zz4Itq63`。
- 最終通知送信完了: `send_manual_full_test_notification.py` はSMTP送信成功。宛先は `stonewebstoneweb@gmail.com`、件名は `[ksrfp-jinjiroumu-blog] 自動実行成功: ok`、通知ログは `07_logs/notifications/latest_notification.json`。
- 最終契約テスト完了: `07_logs/final_run_contract_latest.json` は `status=ok`、`failed_count=0`。WordPress保存、WordPress読み返し検証、Drive保存、状態履歴、通知のmanifest SHA-256一致まで確認済み。
- 自動実行の途中ステータス終了コード改善: `run_weekly_automation.py` は `ok` と処理不要の終端だけを0終了にし、`needs_image_generation_tool` / `needs_drive_upload_plugin` は終了コード2、`partial` / `error` / `blocked_*` は1終了にした。Codex自動実行が途中ハンドオフを「完了」と誤認しないようにした。
- 外部画像後続ランナーの終了コード改善: `continue_after_external_image_sources.py` も `needs_drive_upload_plugin` を0終了にせず2終了にした。Driveプラグイン保存へ続行すべき状態を完了扱いにしない。
- 確認用テキストDrive単体アップロード口の閉鎖: `run_review_text.py --upload` と `build_review_text_file(upload=True)` をブロックした。確認用テキストのDrive保存は、現在manifestのWordPress下書き保存・読み返し検証OK後の `run_review_text_batch_upload.py` またはDriveプラグイン記録経路だけに統一した。
- WordPress保存直前バグ修正: `run_wordpress_publish.py` の一括タイトル重複チェックで `settings` 未定義のまま参照されるバグを修正した。正常な3件ペイロードが揃った本番時にNameErrorで落ちる可能性を潰した。
- WordPress保存コマンドの終了コード厳格化: `run_wordpress_publish.py` は `dry_run` / `created` / `updated_existing` / `already_created` 以外を0終了にしないようにした。`partial`、件数不一致、状態不一致を単体実行でも成功扱いしない。
- 汎用チェックリスト見出し禁止の拡張: `導入前チェックリスト` だけでなく、`申請前のチェックリスト`、`実務チェックリスト`、`導入時のチェックリスト`、`対応前チェックリスト`、`確認チェックリスト` も個別品質チェック、修復、3記事バッチ品質、最終契約テストで禁止・修正対象にした。
- 本番オートメーションprompt再同期: `ksrfp-weekly-run` に、終了コード2の扱い、`run_review_text.py --upload` 禁止、汎用チェックリスト見出し禁止拡張を反映した。スケジュールは毎月1日0:00、実行環境local、モデルGPT-5.5、reasoning effort xhighを維持。
- 追加ローカル検証: `compileall -q src 06_automation` と対象ファイルの `py_compile` は通過。`run_review_text.py --upload` は `blocked_deprecated_upload_entrypoint` で停止、`KSRFP_ALLOW_WORDPRESS_WRITE=1 run_wordpress_publish.py --execute --all` は画像未生成・payload未readyの投稿前プリフライトで `No posts were created` として停止、`run_review_text_batch_upload.py` はWordPress検証NGでDrive保存前停止を確認した。
- 終了コード判定検証: `exit_code_for_payload()` は `ok=0`、`needs_image_generation_tool=2`、`needs_drive_upload_plugin=2`、`partial/error/blocked_preflight_failed=1`、`blocked_all_newsletter_issues_completed=0`、`blocked_concurrent_run=0` を返すことを確認した。
- 未完了バッチの自動再開ロジック追加: `weekly_latest.json` にも現在manifest SHA-256を記録し、前回runが未完了かつmanifest一致の場合に限り、現在の3件バッチを再開対象として判定するようにした。本文・ファクト・画像ゲートがOKならWordPress保存以降を再開し、画像だけ未完了なら `needs_image_generation_tool` としてCodex画像生成へ引き継ぐ。
- 部分下書き時の停止条件改善: `processed_pdfs.json` に `partially_drafted` または必要件数未満の下書きIDがある場合でも、まず現在バッチで再開できるか確認するようにした。再開不能な非画像ゲートNGやmanifest件数不一致のときだけ `blocked_partial_draft_issue` として通知し、同じ号の過剰作成を防ぐ。
- 部分下書き再開の号照合追加: 部分下書きがある場合、現在manifestのPDF名が部分下書きPDFと一致し、既存作成済みトピックキーを現在manifestが含む場合だけ再開を許可するようにした。別号の古いmanifestや別トピックのmanifestを誤って再開しない。
- 再開ロジック検証: `compileall -q src 06_automation` は通過。現状の古い未完成成果物は1件目の本文品質ゲートNGと画像未生成があるため `not_resumable` と判定され、誤ってWordPress保存へ進まないことを確認した。
- 本番オートメーションprompt同期: `ksrfp-weekly-run` の指示へ、前回未完了manifestの再開優先、画像のみ未完了時の `needs_image_generation_tool` 引き継ぎ、再開不能時のみ `blocked_partial_draft_issue` とする仕様を反映した。スケジュールは毎月1日0:00、実行環境はlocal、モデルはGPT-5.5、reasoning effortはxhighのまま維持。
- 部分下書き通知の本文改善: `blocked_partial_draft_issue` の通知本文に、対象PDF、年月、状態、下書き数/必要件数、投稿IDを表示するようにした。要確認時に原因号と残存下書きをすぐ確認できる。
- 最終静的確認: `compileall -q src 06_automation` は通過。旧逃げ道検索（`history_tolerated`、`allow-without-wordpress`、`confirm-initial-test`、`initial_test` 等）は該当なし。最終契約テストは現状未完成成果物を `partial` / NG 55件と判定し、成功扱いしていない。
- 部分下書き状態の新規生成ガード追加: `processed_pdfs.json` に `partially_drafted` または必要件数未満のWordPress下書きIDが残っている場合、次回月次実行でいきなり新規3件生成へ進まず、まず現在manifestで再開できるか判定する。再開不能な場合だけ `blocked_partial_draft_issue` として要確認通知し、プロセス中断後の次回実行で同じ号の下書き過剰作成を防ぐ。
- WordPress投稿前停止の再確認: 現状の未完成成果物に対して `KSRFP_ALLOW_WORDPRESS_WRITE=1 run_wordpress_publish.py --execute --all` を実行し、3件とも `payload_not_ready` / 画像未生成で `Batch WordPress publish preflight failed. No posts were created.` と停止することを確認した。保存ログには現在manifest SHA-256が記録された。
- 最終契約テスト再確認: `verify_final_run_contract.py --allow-missing-notification` は `status=partial`、`failed_count=55`。現在の未完成成果物を成功扱いしていないことを確認した。
- 通知manifest照合追加: 通知結果と送信済みマーカーへ `manifest_sha256` を保存し、最終契約テストで通知payloadが現在の `post_payloads_latest.json` と一致することを確認するようにした。古い通知ログや別run通知を成功根拠にしない。
- Drive PDFゼロのプリフライトNG化: Google DriveフォルダからPDFが0件の `no_drive_pdfs` をOK扱いしないようにした。全号作成済みはDrive PDF一覧が取得できる状態でランナーが通知するが、PDFゼロはソース取得不能として記事生成前に停止する。
- 画像生成後続行時の保存直前プリフライト追加: `continue_after_external_image_sources.py` でも、画像ソース再構築後・WordPress保存前にDrive/WordPress/SMTPプリフライトを再実行するようにした。画像生成で時間が空いた場合も、外部連携NGのままWordPress保存へ進まない。
- WordPressタイトル重複ガードの全件化: 同名タイトル検索で最初の1件だけを見るのをやめ、draft/future/pending/private/publish の完全一致候補を全件確認するようにした。復旧可能な同名下書きがあっても、同名の公開済み記事・由来不明下書き・別トピック下書きが同時にあれば停止する。
- Drive同名ファイル上書きの安定化: Drive APIで確認用テキストと同名ファイルを探す際、`modifiedTime desc` で最新候補を選び、同名候補数を `duplicate_name_count` としてログへ残すようにした。既存同名ファイルがある場合は新規作成ではなく更新を優先する。
- 最終通知のWordPress検証参照先修正: `send_manual_full_test_notification.py` が最終通知判定で参照するWordPress読み返し検証ログを、Drive保存・最終契約テストと同じ `04_wordpress/wordpress_batch_verification_latest.json` に統一した。古い/別名ログによる通知判定ズレを防ぐ。
- WordPress部分成功からの復旧強化: 投稿作成/更新に成功した直後、Arkhe CSS保存前に `08_state/scheduled_posts.json` へ投稿ID・トピックキー・画像ハッシュを記録するようにした。Arkhe CSS保存など後続で落ちても、次回は由来不明の同名下書きとして停止せず、同じトピックの既存下書きを復旧対象として更新できる。
- WordPressメディア再試行の副作用削減: 同じペイロード・同じアイキャッチ画像SHA-256で既存下書きを再処理する場合、既存メディアIDを再利用するようにした。画像が変わった場合だけ新規メディアをアップロードする。メディアアップロード後に投稿作成/更新が失敗した場合は、作成済みメディアの削除も試行する。
- 状態履歴への画像監査情報追加: `scheduled_posts.json` に `featured_image_sha256`、`featured_image_path`、`image_plan_path`、`post_payload_path`、`featured_media_reused` を記録するようにした。最終契約テストでも状態履歴の画像SHA-256が実際の最新アイキャッチ画像と一致することを確認する。
- テーマ重複回避の厳格化: `article_brief.py` の選定処理から、履歴ありテーマを許容するフォールバック経路を削除した。下書き済み・投稿済みテーマは選ばず、3件に足りなければ同じ号で無理に埋めず前の号へ遡る。
- 号状態の巻き戻り防止: 既に `partially_drafted` または `draft_saved_for_review` の号について、生成段階の履歴更新で `analyzed` へ戻さないようにした。途中再開時に下書き済み投稿IDや記事化済みトピック情報を失わない。
- 追加ローカル検証: `compileall -q src 06_automation` は通過。履歴ペナルティ20の高得点テーマがあっても `select_recommended_topics()` が未使用テーマだけを返すことを小テストで確認。現状の未完成成果物は最終契約テストで `partial` のまま、成功扱いされないことも確認した。
- 後工程リトライ強化: `run_weekly_automation.py` と `continue_after_external_image_sources.py` に、WordPress下書き保存、状態補修、WordPress読み返し検証、Drive保存、最終通知のステップ別リトライを追加した。外部一時エラーは最大3回再試行し、決定的な不一致は終端ステータスとして停止する。
- 通知送信リトライ強化: `notification.py` でSMTP/sendmail送信を最大3回再試行するようにした。成功マーカーは実送信後だけ作成するため、通知未送信時は再実行で再送可能。
- manifestハッシュ照合追加: `artifact_fingerprint.py` を追加し、`post_payloads_latest.json` のSHA-256をWordPress保存ログ、WordPress検証ログ、Drive保存メタデータ、最終通知payloadへ記録するようにした。古いログはmtimeだけでは成功根拠にしない。
- WordPress検証のmanifest照合: `run_wordpress_verify_batch.py` は、WordPress保存ログのmanifestハッシュが現在の投稿ペイロードmanifestと一致しない場合、読み返し検証へ進まない。
- Drive保存のmanifest照合: `run_review_text_batch_upload.py` と `record_drive_plugin_uploads.py` は、現在manifestに対するWordPress検証OKが確認できる場合だけDrive保存・Drive URL記録を許可する。
- 状態補修のmanifest照合: `reconcile_wordpress_state_from_publish_log.py` は、現在manifestと一致するWordPress保存ログだけを `08_state` 補修に使う。
- 最終契約テストのmanifest照合: `verify_final_run_contract.py` に、WordPress保存ログ・検証ログ・確認用テキストメタデータのmanifestハッシュ一致チェックを追加した。
- 画像ファイル実体チェック強化: 画像計画、WordPress投稿計画、WordPress投稿直前、最終契約テストで、画像パスは「存在する」だけでなく、通常ファイルかつ非空ファイルであることを必須にした。
- タイトル/構成偏りゲート強化: 3件中2件以上が `中小企業が確認したい` などの定型タイトルに寄る場合、またはまとめ直前H2がチェックリスト型に寄る場合も、3記事バッチ品質NGにするようにした。
- 追加ローカル検証: `compileall -q src 06_automation` と `config/project_settings.json` のJSON検証は通過。現状の未完成成果物では、WordPress検証がmanifest不一致で停止し、Drive保存はWordPress未検証で停止し、最終契約テストは `partial` としてNGを検出することを確認した。
- WordPress実行ガード再確認: 現状の未完成成果物に対して `KSRFP_ALLOW_WORDPRESS_WRITE=1 run_wordpress_publish.py --execute --all` を実行し、投稿前プリフライトで `Batch WordPress publish preflight failed. No posts were created.` として停止することを確認した。保存ログには現在manifestハッシュも記録された。
- 本番オートメーションprompt同期: `ksrfp-weekly-run` のpromptへ、後工程ステップ別リトライ、manifestハッシュ一致必須、3件中2件以上の定型タイトル/チェックリスト型構成NG、画像非空ファイル確認を追記した。スケジュール、モデル、reasoning effort、実行環境は変更なし。
- 同一run作成済み判定の厳格化: `run_wordpress_publish.py` で `wordpress_posts_created` が既に `ok` の場合でも、現在の3件ペイロードと作成済み記録のpayload path、topic key、titleが一致することを確認するようにした。不一致なら `state_payload_mismatch` として重複作成前に停止する。
- Drive保存の逃げ道削除: `run_review_text_batch_upload.py` から `--allow-without-wordpress-verification` を削除した。Google Drive確認用テキスト保存は、常に現在runのWordPress読み返し検証 `status=ok` かつ最新ログであることを必須にした。
- WordPress初期テスト例外の削除: `run_wordpress_publish.py` の `--confirm-initial-test` と、`wordpress_client.py` の初期テスト用ready回避判定を削除した。WordPress送信条件は常に `ready_to_send=true` と画像ゲートOKに統一した。
- 逃げ道削除後のローカル検証: 未完成の現状成果物で `run_wordpress_publish.py --execute --all` を実行し、引き続き `Batch WordPress publish preflight failed. No posts were created.` で停止することを確認した。`run_review_text_batch_upload.py` もWordPress検証 `error` のため `blocked_wordpress_not_verified` で停止した。
- 号選定の遡り改善: 最新の未作成号に3件分の有効テーマがない場合、同じ実行内でその号を `insufficient_viable_topics_in_issue` として一時スキップし、次の未作成号へ遡る `select_newsletter_and_score_until_viable()` を追加した。該当号は作成済み履歴にはしない。
- 号選定ログの改善: `newsletter_issue_selection` に `viability_attempts` と `skipped_insufficient_issues` を記録するようにした。通知メールのラベルも「作成済みとしてスキップ」ではなく「今回スキップした号」に変更し、作成済みスキップと有効テーマ不足スキップを区別できるようにした。
- テーマ選定ポリシーのハードゲート化: `article_brief.py` に `source_policy_violations()` を追加し、ニュース区分、地域限定素材、労働局・労基署の地域ニュース、SEO適性false、編集方針減点40以上、人事労務SEO外の素材を選定不可にした。悪い候補しかない場合は無理にfallbackせず0件にする。
- 選定ルートの統一: `article_writer.py`、`outline_builder.py`、`source_plan.py` の単体実行時も `select_current_or_recommended_topic()` を通すようにし、古いブリーフや単体スクリプト経由でニュース・地域限定素材が混入しないようにした。
- WordPressペイロードへの選定ポリシー反映: `wordpress_payload.py` の `source` に `seo_article_fit`、`editorial_policy_penalty`、`editorial_policy_reason`、`selection_reason`、`policy_violations` を保存し、違反がある場合は `ready_to_send=false` にするようにした。
- 最終契約テストの選定元チェック追加: `verify_final_run_contract.py` で各ペイロードの選定元がニュース区分・地域限定・SEO不適素材ではないことを確認する `payload_N_source_policy_fit` を追加した。本文禁止表現には `制度・ニュースの概要`、`ニュースとして読むだけ` も追加した。
- 3件未満生成の明示ブロック: `build_article_batch()` で生成件数が `articles_per_run` に満たない場合は `insufficient_topics` とし、3記事品質ゲートも `passed=false` にするようにした。`run_weekly_automation.py` では `blocked_insufficient_articles` としてリトライ対象に追加した。
- WordPress部分投稿防止: `run_wordpress_publish.py --all --execute` の開始前に、全3件の `ready_to_send`、画像ファイル、画像ゲート、選定ポリシー違反を一括検査する `validate_batch_payloads_ready()` を追加した。1件でもNGなら「No posts were created」で1件目の作成前に停止する。
- WordPress検証ログの鮮度ゲート追加: `run_wordpress_verify_batch.py` で `wordpress_publish_latest.json` が現在の `post_payloads_latest.json` より古い場合、または検証対象件数が3件でない場合は検証を停止するようにした。古い下書き検証ログを現在runの成功根拠に使わせない。
- ローカル検証追加: `compileall -q src 06_automation` は通過。ニュース区分・地域限定・SEO適性falseの単体ポリシーテストは選定不可、適正候補のみ選定可を確認。未完成の現状成果物に対するWordPress一括投稿は、投稿前プリフライトで `Batch WordPress publish preflight failed. No posts were created.` として停止することを確認した。
- 最終契約テスト再確認: 現在の未完成成果物に対して `verify_final_run_contract.py --allow-missing-notification` を再実行し、`status=partial`、NG 40件として検出した。画像未生成、WordPress保存/検証ログerror、Drive未保存、状態履歴未完了を成功扱いしていない。
- 最終契約テスト追加: `06_automation/verify_final_run_contract.py` を追加した。3件のペイロード、本文禁止表現、3記事品質、毎記事新規写真ソース、WordPress保存ログの最新性、WordPress読み返し検証、Drive保存URL、通知、`08_state` 履歴までを一括判定し、古い成功ログを現在runの成功判定に使わせない。
- 日付契約の厳格化: 最終契約テストでWordPress日付を「9:00」だけでなく「月曜日9:00」として判定するようにした。
- 画像契約の強化: 最終契約テストで、画像ファイルの存在だけでなく `base_image.status=photo_source_copied`、`source_match=fresh_article_source`、`photo_source_fresh=true`、scene profile、alt、プロンプトの新規写真生成要件、シーン種別の分散まで確認するようにした。
- 成果物間の相互照合追加: 最終契約テストで、`post_payloads_latest.json` のmanifestと `post_payload_item_*.json` のタイトル・トピック・ready状態が一致すること、WordPress保存ログのpayload pathと状態履歴のtopic/titleが現在payloadと一致すること、WordPress検証ログのタイトル・投稿IDが現在保存ログと一致することを確認するようにした。
- Driveファイル名・URL契約追加: 最終契約テストで、確認用テキストのファイル名がWordPress下書き日付由来の `YYMMDD 記事タイトル.txt` と一致し、Drive URLにファイルIDを含むことを確認するようにした。
- Drive保存前ゲート強化: `run_review_text_batch_upload.py` で、WordPress読み返し検証ログが現在の `post_payloads_latest.json` より古い場合は `blocked_wordpress_verification_stale` としてDrive保存へ進まないようにした。
- Drive URL記録前ゲート強化: `record_drive_plugin_uploads.py` で、現在runのWordPress検証がOKでない場合はDrive URLをローカル記録しないようにした。Drive URLからファイルIDを取得できない場合も `uploaded` 扱いにしない。
- 通知ログ契約追加: 通知送信結果ログに `run_status`、`run_key`、件名、markerを保存し、最終契約テストで通知が `run_status=ok` の成功通知であることを確認するようにした。
- WordPressカテゴリプリフライト強化: `external_preflight.py` で、WordPress実サイトから取得したカテゴリIDと `config/project_settings.json` の設定カテゴリIDを比較し、不一致がある場合はプリフライトNGとして投稿工程へ進まないようにした。
- WordPress状態履歴の補修追加: `06_automation/reconcile_wordpress_state_from_publish_log.py` を追加した。WordPress下書き保存ログが現在のペイロードに対して最新で成功している場合だけ、`08_state/scheduled_posts.json`、`topic_history.json`、`processed_pdfs.json` を保存ログから補修する。古い保存ログは `stale_publish_log` として補修に使わない。
- 最終通知前ゲート追加: `send_manual_full_test_notification.py` に、通知前の状態履歴補修と `verify_final_run_contract.py --allow-missing-notification` を組み込んだ。最終契約テストがOKでなければ成功通知にせず、`partial` として要確認通知にする。
- 通知後検証追加: 成功通知後に `verify_final_run_contract.py` を再実行し、通知ログ自体が最終成果物より新しいことも確認する。メール本文には最終契約テストのステータスとNG項目名を表示する。
- 通知マーカー改善: 通知済みマーカーのキーに実行ステータスを含めた。同じrunで同じ `partial` 通知が連打されることは防ぎつつ、`partial` 後に自動・手動復旧して `ok` になった場合は成功通知を送れる。
- 後工程への状態補修差し込み: `run_weekly_automation.py` と `continue_after_external_image_sources.py` のWordPress保存直後に `reconcile_wordpress_state_from_publish_log.py` を差し込み、履歴が空のままWordPress検証・Drive保存・最終通知へ進まないようにした。
- ローカル検証: 追加・変更スクリプトの `py_compile` は通過。現在のローカル状態に対して最終契約テストを実行し、画像未生成、WordPress保存/検証ログのstale、Drive未保存、状態履歴未完了を `partial` として検出できることを確認した。
- 画像ツール待ちの専用ステータス化: 写真ソース生成が必要な場合、`run_weekly_automation.py` が `needs_image_generation_tool` を返して中間通知を送らず、Codex画像生成ツールへ引き継ぐようにした。本文品質NGや未確認ファクトがある場合はこのステータスにならず、従来どおり停止する。
- 外部画像後の続行入口追加: `06_automation/continue_after_external_image_sources.py` を追加した。Codex画像生成ツールで3件の `*-featured-photo-source.png` を保存したあと、再構築、WordPress下書き保存、WordPress検証、Drive保存、最終通知まで進める。Drive APIトークンがない場合は `needs_drive_upload_plugin` でGoogle Driveプラグインへ引き継ぎ、中間通知は送らない。
- 本番オートメーション指示の再更新: `ksrfp-weekly-run` に `needs_image_generation_tool` の扱いと `continue_after_external_image_sources.py` の実行を追記した。Codex実行モードでは写真ソース生成をCodex画像生成ツールへ引き継いで続行する経路を明文化した。
- WordPress一括保存前の先行重複チェック: `run_wordpress_publish.py` に3件分タイトルの事前検証を追加した。バッチ内タイトル重複や公開済み同名投稿がある場合は、1件もWordPressへ作成せず停止する。
- WordPress保存前の件数ゲート: `run_wordpress_publish.py` に `articles_per_run` と `post_payload_item_*.json` 件数の一致チェックを追加した。設定値3件と一致しない場合は、1件もWordPressへ作成せず停止する。
- itemファイル参照のmanifest基準化: WordPress下書き保存、Drive確認用テキスト一括保存、Driveプラグイン保存結果記録、最終通知のDrive URL読取で、古い `*_item_*.json` のglob結果ではなく `post_payloads_latest.json` の `items` 件数を正として1〜3件だけ扱うようにした。過去runの残存itemファイルによる件数ズレを防ぐ。
- 通知時刻判定のmanifest基準化: 最終契約テストの `notification_after_final_artifacts` でも、全 `review_text_item_*.json` のglobではなく現在manifestの1〜3件だけを参照するようにした。
- 同名下書き復旧条件の厳格化: 既存の同名 `draft` / `future` / `pending` / `private` 投稿を更新再利用できるのは、`08_state/scheduled_posts.json` に投稿IDと同じトピックキーが記録されている自動化由来の投稿だけにした。状態ファイルにない手動下書き、由来不明下書き、別トピックの同名下書きは上書きしない。
- 外部画像ソース後の再構築: `06_automation/rebuild_after_external_image_sources.py` を追加した。Codex実行モードで組み込み画像生成ツールにより各 `*-featured-photo-source.png` を作成した場合に、3件分の画像計画、WordPressペイロード、確認用テキスト、3記事バッチ品質を再構築できるようにした。
- 本番オートメーションの画像復旧指示: `ksrfp-weekly-run` に、写真ソース生成が必要な場合、Codex画像生成ツールで記事別の新規写真背景を作成し、`rebuild_after_external_image_sources.py` を実行してから後工程へ進む手順を追記した。
- 自動実行の冪等性強化: `run_weekly_automation.py` に実行ロック `07_logs/run_weekly_automation.lock` を追加し、二重起動時は `blocked_concurrent_run` として処理本体へ進まないようにした。WordPress下書き保存直前の二重実行・重複生成リスクを下げた。
- 3記事バッチ品質ゲートの本線化: `article_batch_quality.passed=false` の場合、`blocked_batch_quality` としてWordPress後工程へ進まないようにした。タイトル型の偏り、構成型の偏り、同一記事内反復、アイキャッチ背景重複が残る状態を成功扱いしない。
- WordPress部分成功からの復旧: 既存の同名投稿が `draft` / `future` / `pending` / `private` の場合は、重複作成せず既存投稿を現在のペイロードと新規アイキャッチで更新して再利用するようにした。`publish` の同名投稿はカニバリ防止のため引き続き停止する。
- 最終通知判定の修正: 最終通知メールが送信できても、通知本文の実行ステータスが `partial` の場合は処理全体を `ok` 扱いしないようにした。さらに最終通知スクリプトがすでにメール送信済みの場合は外側ランナーが二重通知しないようにした。
- 通知内容の改善: メール本文に3記事バッチ品質ゲートの要点を追加し、`needs_drive_upload_plugin`、`blocked_batch_quality`、`blocked_concurrent_run` を件名分類上の要確認ステータスとして扱うようにした。
- 本番オートメーション指示の更新: `ksrfp-weekly-run` を更新し、`run_weekly_automation.py` を主入口として後工程を重ね打ちしないこと、`needs_drive_upload_plugin` 時のGoogle Driveプラグイン保存と `record_drive_plugin_uploads.py` 記録、同名下書きの更新再利用、公開済み同名投稿の停止、最終通知1通化を明記した。スケジュール、モデル、実行環境は変更なし。
- 自動実行停止問題の改善実装: 23:12白紙状態テストで露出した `blocked_before_wordpress` を通知して終了する問題に対し、実装修正を開始した。`src/ksrfp_jinjiroumu_blog/article_repair.py` を追加し、H2/H3配下本文量不足、禁止表現、事務所CTA、出典管理語、汎用見出し、同一段落反復を品質ゲートを緩めず自動修復する経路を追加した。
- 画像生成の改善実装: `src/ksrfp_jinjiroumu_blog/image_source_generator.py` を追加し、記事ごとの `featured_image_plan` から新規写真ソースを必要とする状態を検出し、Codex画像生成ツールへ引き継いでから既存のタイトル帯合成・画像ゲートへ戻す経路を追加した。生成済み画像やPIL描画背景の再利用では通らない。
- 画像生成の運用方針: 追加課金を要する画像生成手段は使わず、ChatGPT/Codex側の利用中プラン内で使える画像生成ツールを前提にする。
- 記事生成バッチの自己修復化: `analyze_inputs.py` の `build_article_batch()` に、本文品質NG時の最大3回修復、画像ソース未生成時の最大3回生成、修復後のファクトチェック・品質チェック・WordPressペイロード再生成を組み込んだ。
- ランナーのリトライ制御修正: `06_automation/run_weekly_automation.py` で、例外が出た場合だけでなく、`blocked_before_wordpress` / `blocked_until_verified` のようなゲートNGステータスもリトライ対象に変更した。従来は `run()` が正常終了するとNG判定でも1回目で処理終了していた。
- 状態記録の誤解防止: `state_manager.py` の投稿前状態名を `ready_for_posting` から `generated_ready_before_wordpress` などへ変更し、WordPress下書き未作成のものを作成済み・投稿可能のように見せないようにした。
- ローカル検証: `py_compile` は通過。小規模な本文修復テストで、禁止表現除去、汎用見出し置換、短い見出し本文の補強が動作することを確認した。画像生成プリフライトはCodex画像生成ツールへの引き継ぎ対象として扱い、Python単体の画像生成は成功扱いにしない。
- WordPress後工程の統合: `run_weekly_automation.py` を更新し、生成ゲートが `generation_ready_for_wordpress` になった場合、同じランナー内で `run_wordpress_publish.py --all --execute`、`run_wordpress_verify_batch.py`、`run_review_text_batch_upload.py`、`send_manual_full_test_notification.py` を順に呼ぶようにした。WordPress保存は `KSRFP_ALLOW_WORDPRESS_WRITE=1` をランナー側で付与する。
- Drive確認用テキスト一括保存: `06_automation/run_review_text_batch_upload.py` を追加。WordPress一括検証が `ok` の場合だけ、`review_text_item_*.json` の3件をGoogle Driveへアップロードする。ローカルDrive APIトークンがない場合は `auth_required` を記事単位で記録し、成功扱いにはしない。
- Driveプラグイン引き継ぎ: ローカルDrive APIが未設定で3件すべて `auth_required` の場合、週次ランナーは `needs_drive_upload_plugin` として返し、失敗通知を送らず、Codex Google Driveプラグインで保存を続行するための `next_action` を出すようにした。Driveプラグイン保存後は `06_automation/record_drive_plugin_uploads.py --drive-upload 'ファイル名=Drive URL'` でURLをローカル記録へ反映する。
- Driveプラグイン保存結果記録: `06_automation/record_drive_plugin_uploads.py` を追加。Google Driveプラグインで保存したURLを `review_text_item_*.json` と `07_logs/review_text_drive_plugin_latest.json` に反映し、最終通知メールへDrive URLを載せられるようにした。
- 生成ステータス整理: WordPress前の確認用テキスト未アップロードを `needs_drive_upload` と判定しないよう、`derive_generation_status()` を修正した。Drive保存はWordPress検証後の工程として扱う。
- 後工程のローカル検証: 追加スクリプトと週次ランナーの `py_compile` は通過。`run_review_text_batch_upload.py --allow-without-wordpress-verification` はローカルDrive APIトークン未設定のため3件とも `auth_required` を返すことを確認。`drive_requires_plugin_upload()` は全件 `auth_required` の場合のみ `True` になることを確認した。
- 白紙完全自動実行の納品仕様化: 23:12テストで露出した「写真ソース生成がランナーに組み込まれていない」「品質NG時の自動修復ループがない」問題を、納品判断可能な仕様へ落とし込んだ。`docs/白紙完全自動実行仕様.md` と `docs/白紙完全自動実行テスト計画.md` を追加し、実行主体の境界、状態遷移、画像生成、記事修復、再ファクトチェック、WordPress部分成功時の冪等性、Drive保存、通知重複防止、異常系テストを明文化した。これは仕様化であり、まだ実装完了ではない。
- 白紙状態本番同等テスト（23:12）: `ksrfp-jinjiroumu-blog-blank-state-automation-test` を1回実行した。白紙状態（`03_generated` は `.gitkeep` のみ、`processed_pdfs.json` / `topic_history.json` / `scheduled_posts.json` は空）を確認後、本番 `ksrfp-weekly-run` のpromptに従って外部連携プリフライトを先行実行した。Drive PDF取得、WordPress REST API、カテゴリ取得、SMTP DNS/TCP、SMTPログインはいずれもOK。
- 白紙状態テストの号選定: Google Drive上のPDF一覧と空の履歴から、未作成の最新号として `人事労務だより2026.5月号.pdf`（2026-05）を自律選定した。作成済みとしてスキップした号は0件。手動でPDF号やテーマは指定していない。
- 白紙状態テストの生成結果: 3件生成した。1件目 `最新・行政の動き / パート・有期 改正同一賃金指針を周知 法施行規則と併せ転換制度の公表促進も`、2件目 `助成金情報 / 人材開発支援助成金（事業展開等リスキリング支援コース）`、3件目 `調査 / 「労働時間現状維持希望」多く  働き方改革関連法施行後５年の総点検  厚労省`。
- 白紙状態テストの停止理由: `07_logs/weekly-20260620-231235.json` は `blocked_before_wordpress`。1件目の記事品質が `draft_quality_passed=false` で、H3 `待遇一覧を作る` が199文字、`管理職への共有を行う` が189文字となり200文字以上ゲートに未達。3件すべての画像計画は `requires_fresh_photorealistic_source`、`wordpress_ready=false`、`photo_source_exists=false`。全 `post_payload_item_*.json` が `ready_to_send=false` のため、WordPress下書き保存・WordPress読み返し検証・Google Drive確認用テキスト保存には進んでいない。
- 白紙状態テストの通過項目: 3記事バッチ品質は `passed=true`、`title_uniqueness_ok=true`、`title_pattern_diversity_ok=true`、`structure_pattern_diversity_ok=true`、`intra_article_repetition_ok=true`。禁止表現、事務所CTA、出典管理語、同一記事内の定型段落反復は検出なし。ファクト確認は3件合計で `fact_check_unverified=0`、`publication_gate=verified`。
- 白紙状態テストの通知・記録: `send_manual_full_test_notification.py` を1回だけ実行し、SMTPで最終通知を送信した。通知ログは `07_logs/notifications/notification-20260620-231346.json` と `07_logs/notifications/notification-20260620-231342.eml`。結果は `07_logs/automation_blank_state_test_latest.json`、通知済みmarkerは `07_logs/runs/2026-06-20T23-12-28/notification_sent.json` に記録した。本番オートメーション `ksrfp-weekly-run` とソースコードは変更していない。
- 最終状態（21:15最終クリーン再テスト）: `ksrfp-jinjiroumu-blog-final-clean-retest-2115` は成功。実行runは `2026-06-20T21:17:41`。既存成功成果物である2026.5月号・2026.4月号由来のWordPress下書き6件とGoogle Drive確認用テキスト6件は削除・変更せず保持した。本番の `ksrfp-weekly-run` は変更していない。
- 合格後の追加改善（記事構成）: タイトルとH2構成の固定化を防ぐため、`中小企業が確認したい` 系タイトルの機械的採用を抑制し、`導入前チェックリスト` / `申請前のチェックリスト` / `実務チェックリスト` などの汎用H2を文脈別見出しへ変更した。3記事バッチ品質ゲートへ、タイトル重複、タイトル型の偏り、構成型の偏り、全記事のまとめ直前チェックリスト化の検出を追加した。
- 追加改善のローカル検証: 2026.2月号を対象にローカル生成テストを実施。生成タイトルは `人材開発支援助成金拡充で見直す訓練計画と人事計画`、`出生時育児休業給付金で確認したい産後パパ育休の手続き`、`育児・介護休業の労使協定を自動更新する場合の実務確認`。`title_uniqueness_ok=true`、`title_pattern_diversity_ok=true`、`structure_pattern_diversity_ok=true`、`intra_article_repetition_ok=true`、H2類似度最大 `0.209`、禁止表現なしを確認した。ローカル生成ではAI写真背景を作らないため `image_backgrounds_unique=false` で画像ゲートは想定どおり未通過。
- 本番オートメーション反映: `ksrfp-weekly-run` の指示へ、タイトル型の偏り禁止、汎用チェックリスト見出しの定型利用禁止、`title_uniqueness_ok` / `title_pattern_diversity_ok` / `structure_pattern_diversity_ok` の確認必須を追記した。スケジュール、モデル、実行環境は変更なし。
- 号単位の選定状況（最終）: Google Drive上のPDF一覧と `08_state/processed_pdfs.json` を確認し、`人事労務だより2026.5月号.pdf`（2026-05）と `人事労務だより2026.4月号.pdf`（2026-04）を `draft_saved_for_review` として新しい順にスキップ。未完了号として `人事労務だより2026.3月号.pdf`（2026-03）を選定した。利用可能PDFは2026.5、2026.4、2026.3、2025.1の4件。
- 最終作成テーマ: 1件目 `最新・行政の動き / 改正同一賃金ガイドライン 記載拡充し 10月施行 住宅手当などを追加 厚労省`、2件目 `助成金情報 / 65 歳超雇用推進助成金（高年齢者評価制度等雇用管理改善コース）`、3件目 `身近な労働法の解説 ―女性活躍推進法②一般事業主行動計画― / 女性活躍推進法 1 条に定める「事業主の行動計画の策定」について、一般事業主行動計画の策定・届`。
- 本文反復チェック（最終）: `03_generated/articles/article_batch_quality_latest.json` で `passed=true`、`intra_article_repetition_ok=true`、3件すべて `has_repetition=false`、`repeated_paragraphs=[]`、`repeated_openings=[]`。各 `article_quality_check_item_*.json` は `passed=true`、H2数9、禁止表現0、事務所CTAなし、出典管理語なし。
- 画像品質（最終）: 3件とも記事ブリーフ後に新規生成した写真背景を使用し、`featured_image_plan_item_*.json` は `status=ok`、`base_image.status=photo_source_copied`、`base_image.source_match=fresh_article_source`、`base_image.photo_source_fresh=true`、`photo_source_exists=true`、`wordpress_ready=true`。写真ソースSHA-256は `cd2e7f8132af4d318ba3a795c25b74e98c8230a08d5d8d47e02a8159cea364fb`、`4bc5cdac16f71457d360d0cf0c092af7d1b9cd9208153246cc708ddc9854f79f`、`72a5036bdb3320d282d81575ef9fdd581d0b6cf67214e5ec098317fabd309d78` で重複なし。
- ファクト確認（最終）: 3件とも `fact_check_unverified=0`、`publication_gate=verified`。確認済み一次情報は、厚生労働省 同一労働同一賃金ページ、JEED 65歳超雇用推進助成金ページ、厚生労働省 女性活躍推進法ページ。`fact_check_items_item_*.json` と `post_payload_item_*.json` に確認済みURL、確認日時、確認メモを反映した。
- WordPress下書き（最終）: `run_wordpress_publish.py --all --execute` を `KSRFP_ALLOW_WORDPRESS_WRITE=1` で実行し、投稿ID `4801`、`4803`、`4805` を作成。3件とも `draft`、投稿日 `2026-06-22T09:00:00+09:00`、投稿者ID 2、タグなし、スラッグなし。メディアIDは `4800`、`4802`、`4804`。
- WordPress検証（最終）: `run_wordpress_verify_batch.py` で3件とも `status=ok`。下書き、日付、投稿者、カテゴリ、タグなし、スラッグなし、アイキャッチ、対応画像形式、Arkhe CSS保存、本文H1重複なし（H1数0）、H2/H3あり、禁止表現なしを確認した。
- Google Drive保存（最終）: WordPress検証OK後にGoogle Driveプラグインで指定フォルダ `1vFh7U9F2DhItpYRzVvJ2qRKUpvmOD7jp` へ保存。`260622 同一労働同一賃金ガイドライン改正で中小企業が見直したい待遇説明.txt` は `1o8NxPRG30SIl9txLG01ktAI372icGUBc`、`260622 65歳超雇用推進助成金で中小企業が確認したい高年齢者雇用管理.txt` は `1tk3cbitsdRgzCjnG8Hj4lh5T1s1Z-CQD`、`260622 女性活躍推進法の一般事業主行動計画で中小企業が確認したい実務.txt` は `1qsjSXLzVvKW2XMu3Ihw50ndKSq6Y9VXi`。各 `review_text_item_*.json` と `07_logs/review_text_drive_plugin_latest.json` は `status=uploaded`、method `google_drive_plugin_update_or_upload_after_wordpress_verified`。
- 通知（最終）: `send_manual_full_test_notification.py` を1回実行し、SMTPで最終通知を送信した。ログは `07_logs/notifications/notification-20260620-213605.json` と `07_logs/notifications/notification-20260620-213601.eml`。通知には号選定、スキップ号、各記事の掲載箇所/分類、元トピック、近い既存記事・類似度、WordPress下書きID、Drive URL、画像品質、ファクト確認を含めた。
- 補足修正: 最終通知メールの送信後に、通知本文の開始時刻が古い `latest_run.json` を優先して拾う可能性を確認した。成果物と送信自体に影響はないが、次回以降の通知で当該runの `weekly_latest.json` / WordPress publish log を優先するよう `send_manual_full_test_notification.py` を修正した。重複通知を避けるため、今回通知の再送は行っていない。
- 完了: 245件
- 未完了: 0件
- 合計: 245件
- 最終判定: 白紙状態からの本番相当フルテストは、画像生成ハンドオフ、WordPress下書き保存、Drive確認用テキスト保存、完了通知、最終契約テストまで通過した。現時点の最終状態は成功。

## 1. 目標・運用方針の確定

- [x] 毎月の自動実行日を決める
- [x] 自動投稿の初期ステータスを決める
  - 運用値: `下書き`（`draft`）
  - 投稿時刻: 月曜日9:00
- [x] 1回の実行で作成する記事本数を決める
  - 運用値: 3件
- [x] 投稿カテゴリを決める
- [x] タグ付けルールを決める
- [x] 投稿者アカウントを決める
- [x] スラッグを設定しないルールを決める
- [x] 月曜日9:00の日付設定ルールを決める
  - 運用値: 月次実行後の翌週月曜9:00。3件すべて同じ日付を設定する。
- [x] 既存予約投稿との日付重複回避ルールを決める
- [x] アイキャッチ画像のトーンを決める
- [x] Arkhe CSS Editorに入力するCSSを決める
- [x] 生成記事の最終確認フローを決める
- [x] 自動実行失敗時の通知・確認方法を決める
- [x] 自動実行成功時の通知方法を決める
- [x] 自動実行エラー時のリトライ方針を決める

## 2. 入力データの整理

- [x] Google Search Consoleデータを読み込む
  - 対象: `クエリ.csv`
  - 対象: `ページ.csv`
- [x] Google Analyticsデータを読み込む
  - 対象: `レポートのスナップショット.csv`
- [x] 投稿済み記事一覧CSVを読み込む
  - 対象: `export-all-urls-811885.csv`
- [x] 既存の記事構成プロンプトを読み込む
  - 対象: `柏谷_記事構成_プロンプト.txt`
- [x] 既存の記事本文プロンプトを読み込む
  - 対象: `柏谷_記事本文_プロンプト.txt`
- [x] サンプルの人事労務だよりPDFを読み込む
  - 対象: `人事労務だより2025.1月号.pdf`
  - 対象: `人事労務だより2026.5月号.pdf`

## 3. 既存ブログ・SEO分析

- [x] 投稿済み記事のタイトル・URL・カテゴリを整理する
- [x] 過去記事の主要テーマを分類する
- [x] カニバリ確認用の一覧を作成する
- [x] GSCから流入クエリを整理する
- [x] GSCから表示回数は多いがクリック率が低いクエリを抽出する
- [x] GSCから順位改善余地のあるクエリを抽出する
- [x] GAからよく読まれている記事傾向を整理する
- [x] 記事テーマ選定に使うSEO評価軸を決める

## 4. PDF解析フロー

- [x] PDFから本文テキストを抽出できるか確認する
- [x] PDF内の見出し・話題・ニュース・法改正を抽出する
- [x] 抽出結果から記事候補を複数出せるようにする
- [x] 法改正情報の施行日・適用日を抽出する
- [x] ニュース性のある情報の日付を抽出する
- [x] 中小企業への実務影響を判定する
- [x] 最新号に記事化候補がない場合の過去号選定ルールを作る
- [x] 同じPDFから重複して投稿しない管理方法を決める

## 5. テーマ選定ロジック

- [x] テーマ候補の評価項目を決める
  - 新しさ
  - 正確性確認のしやすさ
  - 中小企業への実務影響
  - SEO需要
  - 過去記事との重複の少なさ
  - 柏谷横浜社労士事務所の業務との相性
- [x] 法改正テーマの一次情報確認ルールを決める
- [x] ニューステーマの鮮度確認ルールを決める
- [x] 過去記事とのカニバリ判定ルールを決める
- [x] 最終的に1テーマを選ぶスコアリング方法を作る

## 6. 記事生成フロー

- [x] 記事構成の生成ルールを作る
- [x] 既存の記事構成プロンプトを自動化用に調整する
- [x] 記事本文の生成ルールを作る
- [x] 1回の実行で3件の記事本文を個別生成する
- [x] 既存の記事本文プロンプトを自動化用に調整する
- [x] タイトル案を複数生成する
- [x] メタディスクリプションを生成する
- [x] WordPress用スラッグを生成する
- [x] 見出し構造を確認する
- [x] 専門用語の説明を入れる
- [x] 中小企業向けの実務対応ポイントを入れる
- [x] 事務所アピール・CTAを入れず、客観的な情報提供トーンにする
- [x] 記事本文に出典管理用の「人事労務だより」表現を出さない
- [x] MarkdownまたはHTMLで記事ファイルを出力する

## 7. 事実確認・品質チェック

- [x] 法改正・制度情報の一次情報を確認する
- [x] 施行日・適用日・受付期間などの日付を確認する
- [x] 古い情報を参照していないか確認する
- [x] 元調査資料が未特定の数値を未確認として残す
- [x] 法律・制度・日付・数値の確認項目を抽出する
- [x] 確認項目ごとに公的ソース候補URLを出力する
- [x] 確認済み根拠レジストリをファクトチェックへ反映する
- [x] 未確認の根拠項目が残る場合は公開可能扱いにしない
- [x] 下書き品質判定と公開可能判定を分ける
- [x] 過去記事と内容が重複していないか確認する
- [x] 読者である中小企業経営者・人事労務担当者に役立つ内容か確認する
- [x] 事務所の専門領域と自然につながっているか確認する
- [x] 過度な断定やリスクのある表現がないか確認する
- [x] SEOタイトル・本文・見出しが自然か確認する
- [x] `人事労務だより`、`出典PDF`、`掲載されていた` などの出典管理語を禁止表現チェックに追加する
- [x] 同一記事内で同じ段落・定型文が反復していないかを検出し、反復があればWordPress保存へ進めない

## 8. アイキャッチ画像生成

- [x] アイキャッチ画像の基本サイズを決める
- [x] アイキャッチ画像のデザイン方針を決める
- [x] 記事テーマに合わせた画像プロンプトを生成する
- [x] 画像を生成する
- [x] 画像ファイル名を決める
- [x] 代替テキストを生成する
- [x] WordPressにアップロードできる形式で保存する
- [x] 3件のアイキャッチ背景が同一にならないよう、記事別の生成画像へ差し替える
- [x] 写真品質の背景画像を必須にし、コード生成の簡易イラスト背景ではWordPress下書き保存へ進めない
- [x] 写真背景ソースの有無を画像計画に記録し、簡易背景・プレースホルダーを不合格にする
- [x] 過去画像・既存画像・同テーマ画像の再利用を禁止し、記事ブリーフ作成後に新規生成された写真ソースだけを許可する
- [x] 記事タイトル・元トピック・PDF内区分・ラベル・素材抜粋から画像シーンを分岐する
- [x] 既存下書き6件で、新仕様の毎記事新規画像生成・差し替え・WordPress再取得検証を実施する

## 9. WordPress API連携

- [x] アプリケーションパスワード情報ファイルをローカルで検出する
- [x] アプリケーションパスワード情報をローカルから読み取る
- [x] WordPress REST APIの接続確認をする
- [x] 投稿カテゴリ一覧を取得する
- [x] タグは設定しない前提でタグ一覧の読み取り確認をする
- [x] メディアアップロードをテストする
- [x] WordPress下書き保存をテストする
- [x] 3件分のWordPress下書き保存ドライランをテストする
- [x] 3件分のWordPress下書き保存を実行しREST APIで確認する
- [x] 投稿本文のHTML表示を確認する
- [x] Arkhe CSS EditorのREST設定キーを確認し、`arkhe_css_front` / `arkhe_css_editor` に反映する
- [x] アイキャッチ画像の設定を確認する
- [x] カテゴリ・タグの反映を確認する
- [x] 投稿URLを取得してログに保存する

## 10. Googleドライブ連携

- [x] Googleドライブへのアクセス方法を決める
- [x] 指定フォルダ内のPDF一覧を取得する
- [x] 最新号を判定する
- [x] 最新号のPDFをダウンロードする
- [x] 処理済みPDFを記録する管理ファイルを作る
- [x] 処理済みPDFを記録する
- [x] 最新号がない場合の過去号ランダム選定を実装する
- [x] Googleドライブ連携の失敗時処理を決める

## 11. 自動化

- [x] Codexオートメーションで毎月1日0:00実行するスケジュールを設定する
- [x] 自動実行時の入力・出力パスを決める
- [x] 実行ログの保存先を決める
- [x] 処理済みPDF・投稿済みテーマの管理ファイルを作る
- [x] 途中失敗時に再実行できる設計にする
- [x] 月次自動実行用ラッパースクリプトを作る
- [x] 3件分の生成物を `item_1` / `item_2` / `item_3` として保存する
- [x] 成功・失敗を問わないメール通知処理を作る
- [x] 通知メールにブログ記事のテーマ元となった人事労務だよりの号・掲載箇所・元トピックを記載する
- [x] 通知メールに3件分のテーマ元・重複確認・確認用テキスト・Drive保存状況を記載する
- [x] WordPress下書き保存後の最終通知メールに投稿ID・アイキャッチ・画像品質ゲート結果を記載する
- [x] SMTP通知設定ファイルを配置する
- [x] SMTPサーバーへの実送信テストを通す
- [x] メール通知の受信箱到達を確認する
- [x] 自動実行のWordPress送信直前にアイキャッチ写真品質ゲートを通す
- [x] 本番オートメーション指示に写真品質ゲートと写真ソース生成手順を反映する
- [x] 手動実行と同じローカル環境で自動実行テストを起動する設定にする
- [x] WordPress下書き保存までの自動実行をテストする
- [x] 本番運用前のリハーサルを実施する
- [x] GitHubの成功実装 `westhill-blog-automation` / `wordpress-article-automation` を確認し、採用すべき設計差分を整理する
- [x] 自動実行冒頭で、WordPress REST API読み取り・カテゴリ取得・SMTP DNS/TCP・SMTPログインを同一実行環境で確認するプリフライトを必須化する
- [x] 外部成果物の作成順序を、WordPress保存・読み返し検証後にGoogle Drive確認用テキスト保存へ進む形に変更する
- [x] run単位の `state.json` を導入し、既にWordPress下書き作成済みのrunで二重投稿しないようにする
- [x] 最終通知に `notification_sent.json` marker を導入し、同一runで成功通知を重複送信しない
- [x] SMTP失敗時はsendmail成功を前提にせず、SMTP主経路・emlバックアップ・ログ記録を本番通知仕様として整理する
- [x] 上記修正後、手動補修なしの本番同等自動実行テストを再実施する

## 12. 初回リリース判定

- [x] 1本の記事をローカルで生成できる
- [x] テーマ選定理由を説明できる
- [x] 過去記事との重複確認ができる
- [x] 法改正・ニュースの事実確認ができる
- [x] アイキャッチ画像を生成できる
- [x] WordPressに下書き保存できる
- [x] 3件分のWordPressペイロードを作成できる
- [x] GoogleドライブからPDFを取得できる
- [x] 旧テスト生成分を削除し、手動フル実行で3件を新規作成できる
- [x] 初回の定期実行後に実績確認できる
- [x] 実行ログを確認できる
- [x] 失敗時の原因を追跡できる

## 13. 確認用テキストファイル

- [x] 添付サンプルのテキスト形式を確認する
- [x] 記事タイトルと記事本文を分けた確認用テキストを生成する
- [x] WordPress本文側のH1重複回避と同じく、確認用本文からMarkdownのH1を除外する
- [x] ファイル名を `YYMMDD 記事タイトル.txt` 形式にする
- [x] ファイル名の日付をWordPress下書き日付と同じにする
- [x] 生成先を `03_generated/review-texts/` にする
- [x] Googleドライブ保存先フォルダIDを設定に追加する
- [x] Drive APIトークンがある場合はアップロードし、ない場合は `auth_required` として止めずに記録する
- [x] 指定Googleドライブフォルダへの実保存を確認する
- [x] 3件分の確認用テキストを指定Googleドライブフォルダへ保存する
- [x] 既存のDrive確認用テキスト3件を同一ファイルIDのまま上書き更新する

## 14. SEO記事品質の再設計

- [x] 初期要望を最上位仕様として再定義する
- [x] 手動の記事構成プロンプトを記事構成仕様へ反映する
- [x] 手動の記事本文プロンプトを本文品質仕様へ反映する
- [x] ニュース区分を原則除外する編集方針を実装する
- [x] 山口県・福島県など地域限定情報を原則除外する編集方針を実装する
- [x] 人事労務SEOから外れる素材を除外する編集方針を実装する
- [x] 生成済みテスト履歴で良質テーマが不当に落ちないよう、履歴減点前の実務価値も見る選定補完を実装する
- [x] H2 7個以上、最初のH2「はじめに」、最後のH2「まとめ」を構成生成に固定する
- [x] 「事例」「成功例」を禁止表現に追加する
- [x] 各H2/H3/H4ブロックが200文字以上かを品質チェックする
- [x] 3記事間の「はじめに」「まとめ」「H2構成」類似度をチェックする
- [x] 同一背景・同一構成・同一まとめにならないよう、バッチ品質ログを出力する
- [x] 未確認ファクトが残る場合はWordPress下書き保存にも進めないゲートにする
- [x] Drive APIが `auth_required` の場合にGoogle Driveプラグインで保存する具体手順を自動化指示へ反映する
- [x] 最終通知で古いWordPress保存ログを拾わないよう、最新ペイロードより古いログを無効扱いにする
- [x] WordPress保存直前に同一タイトルの既存投稿を検出し、重複下書き作成を停止する
- [x] Google Drive保存で同名ファイルがある場合は新規作成ではなく上書き更新する
- [x] 生成段階の中間通知を抑止し、最終通知中心の運用にする
- [x] 号単位の作成済み履歴を記録し、最新号が作成済みなら前号へ遡る選定仕様を実装する
- [x] 全号作成済みの場合に記事作成を停止し、失敗通知へ号選定状況を記載する
- [x] 労働時間管理の記事本文を個別生成する
- [x] 高年齢者の労働災害防止の記事本文を個別生成する
- [x] 社宅貸与と現物給与の記事本文を個別生成する
- [x] 安全衛生・社宅給与計算のアイキャッチを専用背景へ分岐する
- [x] 新仕様3件のWordPress下書き保存ドライランを3件すべて `ready` で通す
- [x] 既存WordPress下書き4件を削除し、新仕様の下書き3件を新規作成する
- [x] 既存Google Drive確認用テキスト4件を削除し、新仕様の確認用テキスト3件を新規保存する
- [x] 新仕様の手動フル実行で、生成・Drive保存・WordPress下書き・検証・通知まで完了する
- [x] 新仕様でWordPress下書き保存までの自動実行テストを実施する
- [x] 修正後コードで完全無介入のクリーン自動実行テストを通す
- [x] 同一写真ソース重複ゲート修正後、本番同等の最終自動実行テストを再実施して合格判定する

## メモ

- 初期運用からWordPress投稿ステータスは `下書き`（`draft`）にする。日付は月次実行後の翌週月曜9:00を3件すべてに設定する。未確認ファクトが残る場合は、下書き保存であってもWordPressへ進めない。
- 法改正や制度情報は、公的機関・一次情報で確認する。
- テーマ選定では、最新性だけでなく中小企業への実務影響と過去記事との重複回避を重視する。
- 2026-06-19に投稿ID `4712` を作成し、アイキャッチ、カテゴリ、タグなし、本文表示、Arkhe CSS Editor反映を確認した。ユーザー確認後、投稿ステータスは下書き保存へ変更する。
- 2026-06-19 18:40に投稿ID `4712` を `draft` に変更し、設定日時は `2026-06-22T09:00:00` のまま維持した。
- 2026-06-19 18:40に本文を客観トーン版へ更新し、品質チェックで「事務所アピール・CTAなし」「禁止表現なし」を確認した。
- 2026-06-19に週次オートメーション `ksrfp-weekly-run` の指示文を、WordPress下書き保存前提へ更新した。
- 2026-06-19 18:50にGmail受信箱で通知メール到達を確認した。
- 2026-06-19 18:57にアイキャッチ画像のタイトル中央配置ルールを実装し、投稿ID `4712` のアイキャッチをメディアID `4717` に差し替えた。
- 2026-06-19に「運用後の改善」は開発段階のチェック対象から除外し、`00_project/運用後改善チェックシート.md` へ移行した。
- 2026-06-19に確認者向けテキストファイル生成を追加した。保存先Google Driveフォルダは `1vFh7U9F2DhItpYRzVvJ2qRKUpvmOD7jp`。
- 2026-06-19 19:42にGoogle Driveプラグイン経由で確認用テキストを指定フォルダへ保存し、DriveファイルID `1dGP6x60T0bUUBNrYnDLRJ68a5m20Ke8O` を確認した。
- 2026-06-19 19:49に20:00実行の一時自動実行テスト `ksrfp-jinjiroumu-blog-20-00-automation-test` を作成した。既存の毎週月曜0:00本番オートメーション `ksrfp-weekly-run` は変更しない。
- 2026-06-19 19:49に20:10結果確認用フォローアップ `ksrfp-20-00-automation-test-result-check` を作成した。
- 2026-06-19 20:16時点で20:00テストのログ更新がなく、同一ワークスペースで待機プロセスを動かしていたことによる競合の可能性があるため、20:00テストと20:10確認を削除した。
- 2026-06-19 20:17にワークスペースを空けた状態で再検証するため、20:25実行の一時自動実行再テスト `ksrfp-jinjiroumu-blog-20-25-automation-retest` と20:35結果確認 `ksrfp-20-25-automation-retest-result-check` を作成した。
- 2026-06-20 04:03に20:25再テストもログ更新がなく、原因は一時テストにCodex cronの想定外である `FREQ=DAILY;COUNT=1` を使った時刻指定ミスの可能性が高いと確認した。20:25再テストと20:35確認は削除した。
- 2026-06-20 04:04にCodex cronが想定する週次形式で、04:15実行テスト `ksrfp-jinjiroumu-blog-saturday-04-15-automation-test` と04:30結果確認 `ksrfp-saturday-04-15-automation-test-result-check` を作成した。
- 2026-06-20 04:32に04:15実行テストを確認したが、`07_logs/weekly_latest.json` は2026-06-19 17:47:15、通知ログも2026-06-19 17:47:15のままで、04:15のworktree cronは発火していないと判定した。結果は `07_logs/automation_test_0415_latest.json` に記録した。
- 2026-06-20 04:32時点では、heartbeatによる04:30結果確認は発火しているため、スレッドへのフォローアップは機能しているが、worktree cronの自動実行が期待どおり動いていない。OSレベルの `launchd` など、Codex外部のスケジューラで同一コマンドを実行する方式を検討する。
- 2026-06-20 04:53に、発火実績のあるheartbeat方式で05:00フル自動実行テスト `ksrfp-heartbeat-05-00-full-automation-test` を作成した。週次実行、品質チェック、確認用テキストDrive保存、WordPress重複回避・検証、通知、進捗記録までをテスト対象にする。
- 2026-06-20 04:56に、自動実行通知メールへ「今回のテーマ元」ブロックを追加した。表示項目は人事労務だよりの号、掲載箇所/分類、元トピック、テーマ管理キー、ラベル、日付言及、元記事抜粋、近い既存記事URL・類似度。
- 2026-06-20 05:38に、実行頻度を毎週月曜0:00から毎月1日0:00へ変更した。Codexオートメーション `ksrfp-weekly-run` は名称を月次運用へ更新し、RRULEも月次に変更済み。
- 2026-06-20 05:38に、1回の実行で3件の記事候補を生成するよう実装した。生成物は `article_draft_item_1.md`、`post_payload_item_1.json`、`review_text_item_1.txt` のように個別保存する。
- 2026-06-20 05:38に、3件すべてのWordPress下書き日付と確認用テキストファイル名の日付を `2026-06-22T09:00:00+09:00` / `260622` で確認した。
- 2026-06-20 05:38に、3件分の確認用テキストをGoogle Driveへ保存した。DriveファイルIDは `1MdxIS3bDD18LjYret5ZdgTXxq8I77vcG`、`112Ju4VK6t7JswWgQ2yj6kQlqjhgUJkp4`、`18tV8kpRqPpYtGyOQWE7eOJQcKijPii5X`。
- 2026-06-20 05:38に、`run_wordpress_publish.py --all` のドライランで3件すべて `ready` を確認した。カテゴリは助成金系が `助成金`、メンタルヘルス系が `労務管理`。
- 2026-06-20 05:41に、3件分のWordPress下書き保存を実行した。投稿IDは `4719`、`4722`、`4725`。REST APIで3件とも `draft`、日付 `2026-06-22T09:00:00`、投稿者ID `2`、タグなし、スラッグなし、アイキャッチありを確認した。
- 2026-06-20 05:41に、実サイトAPIでカテゴリIDを確認した結果、助成金は ID `5`、ライフプランニングは ID `13` だった。ユーザー提供一覧とは異なるため、WordPress APIの実値を優先する。投稿ID `4719` と `4725` は助成金 ID `5` へ修正済み。
- 2026-06-20 05:42に、3件版テスト結果の通知メールをSMTPで送信した。送信結果は `07_logs/manual_3_article_test_latest.json` と `07_logs/notifications/latest_notification.json` に記録した。
- 2026-06-20 06:13に、投稿ID `4719`、`4722`、`4725` の本文を修正版へ上書きし、アイキャッチをメディアID `4727`、`4729`、`4731` へ差し替えた。
- 2026-06-20 06:29に、Google Drive上の確認用テキスト3件を同一ファイルIDのまま上書き更新した。対象ファイルIDは `1MdxIS3bDD18LjYret5ZdgTXxq8I77vcG`、`112Ju4VK6t7JswWgQ2yj6kQlqjhgUJkp4`、`18tV8kpRqPpYtGyOQWE7eOJQcKijPii5X`。
- 2026-06-20 06:31に、Arkhe CSS Editorの保存先を投稿メタではなくREST設定 `arkhe_css_front` / `arkhe_css_editor` と確認し、既存CSSを保持したまま指定CSSを追記した。今後のWordPress投稿処理も同方式に変更済み。
- 2026-06-20 06:33に、投稿ID `4719`、`4722`、`4725` をREST APIで読み戻し、本文に `人事労務だより`、`出典PDF`、`掲載されていた`、事務所CTAが残っていないことを確認した。
- 2026-06-20 06:38に、PDF抽出文の粗い断片（例: `・OFF`、不自然な改行、途中で切れた日付）が概要本文に出ないよう、テーマ別の自然な概要文生成へ変更した。3件目タイトルは `山口県の正社員化支援とは？中小企業が確認したい雇用管理と奨励金のポイント` に修正した。
- 2026-06-20 06:38に、投稿ID `4719`、`4722`、`4725` を再上書きした。最終アイキャッチメディアIDは `4733`、`4735`、`4737`。
- 2026-06-20 06:44に、最終検証として投稿ID `4719`、`4722`、`4725` をREST APIで読み戻し、禁止表現・粗い抽出表現なし、`draft`、日付 `2026-06-22T09:00:00`、Arkhe CSS反映済みを確認した。検証結果は `04_wordpress/wordpress_revision_verification_latest.md`。
- 2026-06-20 07:18に、初期要望と手動プロンプト準拠の新仕様でローカル生成を再実行した。生成テーマは `調査 / 労働時間管理`、`身近な労働法の解説 / 高年齢者労災防止`、`実務に役立つQ&A / 社宅貸与と現物給与`。3件すべて下書き品質OK、各見出し200文字以上、禁止表現なし、3記事類似チェックOK。
- 2026-06-20 07:21に、アイキャッチ画像を `working-hours-item-1-featured.png`、`occupational-safety-item-2-featured.png`、`payroll-housing-item-3-featured.png` に分岐し、WordPress下書き保存ドライランで3件すべて `ready` を確認した。
- 2026-06-20 07:45に、旧生成分のGoogle Drive確認用テキスト4件を削除し、Drive保存先フォルダが空になったことを確認した。
- 2026-06-20 07:45に、旧WordPress下書き4件（投稿ID `4712`、`4719`、`4722`、`4725`）をREST APIで削除した。削除結果は `04_wordpress/wordpress_delete_result_latest.md`。
- 2026-06-20 07:45に、ローカル生成物と状態履歴をリセットした。対象は `03_generated/` 配下の生成記事・画像・確認用テキスト・payload と `08_state/` の履歴ファイル。
- 2026-06-20 07:47に、実サイトAPIカテゴリに合わせて `life-planning=13`、`subsidy=5` を設定へ反映し、再生成を実行した。
- 2026-06-20 07:47に、新仕様で3件を生成した。テーマは `調査 / 労働時間管理`、`助成金情報 / 人材開発支援助成金`、`身近な労働法の解説 / 高年齢者労災防止`。3件すべて下書き品質OK、禁止表現なし、3記事類似チェックOK。
- 2026-06-20 08:18に、確認用テキスト3件をGoogle Driveへ新規保存した。DriveファイルIDは `1LYhQ4zJ_SpbPJQ-J7iwSuETK-wSJqTc1`、`1U4LO5EoQPSqANqqA0Qrykd-CtkMLAcgN`、`1goisI4-sIIfQMPmFs-L_Eqj37B4y1eoE`。
- 2026-06-20 08:18に、WordPress下書き3件を新規保存した。投稿IDは `4740`、`4742`、`4744`。3件とも `draft`、日付 `2026-06-22T09:00:00`、投稿者ID `2`、タグなし、スラッグなし、アイキャッチあり、Arkhe CSS反映済みを確認した。
- 2026-06-20 08:20に、3件一括検証スクリプトでWordPress下書き・日付・投稿者・カテゴリ・タグなし・アイキャッチ・本文H2/H3・Arkhe CSS・禁止表現なしをすべてOKと確認した。結果は `04_wordpress/wordpress_batch_verification_latest.md`。
- 2026-06-20 08:21に、本番同様の通知メールをSMTPで送信した。通知には各記事の元PDF、掲載箇所/分類、元トピック、WordPress下書きID/URL、Drive URLを記載した。
- 2026-06-20 08:24に、手動フル実行成功後の自動実行テスト用フォローアップ `ksrfp-manual-success-follow-up-automation-test` を作成した。手動テスト成果物は削除せず、自動起動後に同品質でDrive保存、WordPress下書き、検証、通知まで進むかを確認する。
- 2026-06-20 09:00に、アイキャッチ背景が写真品質ではなくコード生成の簡易イラスト背景になっていた原因を確認した。原因は `image_plan.py` の `generate_thematic_background()` が写真生成の代替ではなく自動フォールバックとして動いていたこと。
- 2026-06-20 09:00に、自動実行テスト用フォローアップ `ksrfp-manual-success-follow-up-automation-test` を削除した。写真品質ゲートを入れる前に自動実行を進めないため。
- 2026-06-20 09:02に、3件のアイキャッチを記事内容に合う写真品質背景へ差し替えた。新メディアIDは投稿ID `4740` が `4745`、投稿ID `4742` が `4747`、投稿ID `4744` が `4749`。
- 2026-06-20 09:02に、タイトル改行ロジックへ句読点・疑問符の行頭回避と意味単位改行候補を追加した。`人材開発支援助成金とは？` が不自然に分割されないことを確認した。
- 2026-06-20 09:02に、WordPress一括検証で3件とも `draft`、日付、投稿者、カテゴリ、タグなし、スラッグなし、アイキャッチ、Arkhe CSS、禁止表現なしをOK確認した。検証結果は `04_wordpress/wordpress_batch_verification_latest.md`。
- 2026-06-20 09:02に、画像生成仕様を「写真品質必須」に変更した。写真背景がない場合は `requires_photorealistic_source` とし、コード生成の簡易イラスト背景ではWordPress下書き保存へ進めない。
- 2026-06-20 09:10に、画像品質ゲートを `image_gate.py` として共通化し、画像計画、WordPressペイロード、WordPress書き込み直前、既存下書きアイキャッチ差し替え処理のすべてで同じ判定を使うようにした。
- 2026-06-20 09:10に、3件分の画像計画とWordPressペイロードを再生成し、すべて `base_status=photo_source_copied`、`photo_source_exists=true`、`ready_to_send=true`、WordPress投稿ドライラン `ready` を確認した。
- 2026-06-20 09:11に、簡易背景ステータスなら停止理由 `アイキャッチ背景が写真品質ではありません。AI生成の写真背景ソースを作成してください。` が出ることを単体確認した。
- 2026-06-20 09:11に、`compileall` とWordPress一括検証を実行し、3件とも下書き・日付・投稿者・カテゴリ・タグなし・スラッグなし・アイキャッチ・Arkhe CSS・禁止表現なしがOKであることを確認した。
- 2026-06-20 09:14に、本番オートメーション `ksrfp-weekly-run` の指示へ、写真品質背景ソース必須、`photo_source_copied` / `photo_source_exists=true` / `wordpress_ready=true` 確認後のみWordPress下書き保存へ進む条件を追加した。スケジュールは毎月1日0:00のまま維持した。
- 2026-06-20 09:16に、本番オートメーション `ksrfp-weekly-run` の実行環境を `worktree` から `local` へ変更した。プロジェクトが現状git未追跡ファイル群で構成されているため、手動実行と自動実行で別コードになるリスクを避けるため。
- 2026-06-20 09:17に、自動実行テスト `ksrfp-jinjiroumu-blog-automation-test-0925` を `2026-06-20 09:25 JST` 発火予定で作成した。結果確認フォローアップ `ksrfp-automation-test-0925-result-check` は `2026-06-20 10:20 JST` に設定した。
- 2026-06-20 09:20に、下書き保存後の最終通知スクリプトでDrive保存結果と画像品質ゲート結果を自動取得できるようにした。本番・テストのオートメーション指示にも `send_manual_full_test_notification.py` 実行を追加した。
- WordPress本文ではテーマ側の投稿タイトルと本文H1が重複しないよう、投稿本文HTML生成時にMarkdownのH1を除外する。
- 2026-06-20 09:38に、自動実行テスト `ksrfp-jinjiroumu-blog-automation-test-0925` を本番同等手順で完了した。初回生成後に画像品質ゲートで停止し、AI生成の写真品質背景ソースを3テーマ分作成して `photo_source_copied` / `photo_source_exists=true` / `wordpress_ready=true` / `ready_to_send=true` を確認した。
- 2026-06-20 09:38に、Google Drive確認用テキスト3件を指定フォルダへ保存・更新した。DriveファイルIDは `1LYhQ4zJ_SpbPJQ-J7iwSuETK-wSJqTc1`、`1U4LO5EoQPSqANqqA0Qrykd-CtkMLAcgN`、`10uJzkXThrColF3wZpFBgbfr1vA0mdr69`。
- 2026-06-20 09:38に、WordPress下書き3件を新規保存した。投稿IDは `4752`、`4754`、`4756`。3件とも `draft`、日付 `2026-06-22T09:00:00`、投稿者ID `2`、カテゴリ一致、タグなし、スラッグなし、アイキャッチあり、Arkhe CSS反映済み、禁止表現なしを一括検証で確認した。
- 2026-06-20 09:38に、REST取得本文の追加確認で3件とも本文H1が `0` 件であることを確認した。最終通知メールはSMTPで送信済み。結果は `07_logs/automation_test_0925_latest.json` に記録した。
- 2026-06-20 09:45に、アイキャッチの古い `*-featured-base.png` キャッシュが残る問題を修正し、現在の写真ソースから3件を再合成した。WordPress側の最終メディアIDは `4757`、`4759`、`4761`。
- 2026-06-20 09:45に、アイキャッチ差し替え後の一括検証を再実行し、3件とも `draft`、日付、投稿者、カテゴリ、タグなし、スラッグなし、最新アイキャッチ、Arkhe CSS、禁止表現なしをOK確認した。最新アイキャッチURLと投稿IDを含む最終通知メールもSMTPで再送済み。
- 2026-06-20 10:07に、09:55本番同等テストが最終成果物まで到達した。生成テーマは `調査 / 労働時間管理`、`助成金情報 / 人材開発支援助成金`、`身近な労働法の解説 / 高年齢者労災防止`。3件とも品質OK、未確認ファクト0、画像ゲート `photo_source_copied`、Drive保存 `uploaded`、WordPress下書きID `4764`、`4766`、`4768`、メディアID `4763`、`4765`、`4767`。一括検証、最終通知まで完了したが、実行中に通知・判定コード補強が入ったため、完全無介入合格ではなく `needs_clean_retest` として `07_logs/automation_test_0955_latest.json` に記録した。
- 2026-06-20 10:10に、Drive API `auth_required` 時のGoogle Driveプラグイン保存手順、未確認ファクト停止ゲート、古いWordPressログ無効化、最終通知判定を本番・テスト自動化へ反映し、10:20に完全無介入のクリーン再テストを設定した。
- 2026-06-20 10:30に、10:20本番同等クリーン自動実行テストを合格判定した。3件とも記事品質・類似度・禁止表現なし・未確認ファクト0・写真品質アイキャッチ・Drive保存・WordPress下書き・一括検証・H1重複なし・通知送信を確認した。投稿IDは `4770`、`4772`、`4774`、DriveファイルIDは `14O0zQrW67-WzIXabLakAvmTgKRsXxwsd`、`1MCXSGDUMcRdZn-x_tfQn7_IDXcP9hKat`、`1zYRWbLPVmFJt3nfTCAnVdfMaVgHeFZSx`。結果は `07_logs/automation_test_1020_latest.json` に記録した。
- 2026-06-20 13:55に、重複下書き・Drive件数差・通知過多の原因を調査した。原因は、複数回のテスト成果物を残したこと、WordPress保存直前の同名タイトルガード不足、Driveプラグイン保存時の新規作成混在、生成段階通知と最終通知の二重送信。修正として、WordPress重複ガード、Drive同名ファイル上書き、生成段階通知抑止、本番自動化指示のDrive update優先を反映した。
- 2026-06-20 13:58に、最終検証前のリセットを実施した。Google Drive確認用テキスト保存先は空、WordPressの今回テスト由来下書き `4740`、`4742`、`4744`、`4752`、`4754`、`4756`、`4764`、`4766`、`4768`、`4770`、`4772`、`4774` は削除済み。ローカル生成物は `03_generated/` 配下125ファイルを削除し、`08_state/` の重複判定・処理履歴を初期化した。

- 2026-06-20 10:27に、10:20本番同等クリーンテストを完了した。生成テーマは `調査 / 労働時間管理`、`助成金情報 / 人材開発支援助成金`、`身近な労働法の解説 / 高年齢者労災防止`。3件とも品質OK、未確認ファクト0、画像ゲート `photo_source_copied`、Drive保存 `uploaded`、WordPress下書きID `4770`、`4772`、`4774`、メディアID `4769`、`4771`、`4773`。一括検証とH1追加検証、SMTP通知まで成功し、結果を `07_logs/automation_test_1020_latest.json` に記録した。既存本番オートメーション `ksrfp-weekly-run` は変更していない。
- 2026-06-20 16:15に、本番オートメーション `ksrfp-weekly-run` と同一プロンプト・同一作業フォルダ・同一実行環境で二回目の最終検証を実行した。実行は発火し、`人事労務だより2026.5月号.pdf` を作成済みとしてスキップし、`人事労務だより2026.4月号.pdf` から3件を生成した。WordPress下書きIDは `4782`、`4784`、`4786`、Drive保存ファイルIDは `1zwOiiFoqGleWUpAl34q9G_Ky2cL3OoRe`、`1oDpGkuzYehqOgMIfbHxm06Oe0nm5lXk5`、`1sE3J1AcofbAIh5AAwbzjUWfD9wuzN_cP`。通知メールは16:36に1通のみ送信され、中間通知は発生しなかった。
- 2026-06-20 16:50に、二回目の最終検証で1件目と3件目のアイキャッチ背景写真ソースが同一であることを確認した。これは「記事内容に合った画像を毎回生成する」仕様に反するため、最終検証は不合格と判定した。
- 2026-06-20 16:54に、`run_wordpress_publish.py --all` へ同一写真ソース重複ゲートを追加し、現在の重複状態ではWordPress保存前に停止することを確認した。`analyze_inputs.py` のバッチ品質ログにも写真ソース重複検出を追加した。`compileall` は成功。
- 2026-06-20 16:58に、3件目「無期転換ルール」の写真背景ソースを契約更新実務に合う新規写真へ差し替え、投稿ID `4786` のアイキャッチをメディアID `4787` へ更新した。差し替え後の一括検証では3件とも `status=ok`。ただし、これは自動実行後の補修であるため、最終検証合格とは扱わず、修正後に再度本番同等自動実行テストが必要。
- 2026-06-20 17:00に、画像生成仕様をさらに厳格化した。`image_plan.py` から同テーマ・同名・過去写真ソースを探索して再利用する経路を削除し、記事ブリーフ作成後に更新された写真ソースだけを `fresh_article_source` として許可する。古い写真ソースは `stale_photo_source_rejected`、既存アイキャッチ画像は `existing_image_rejected` としてWordPress保存前に停止する。
- 2026-06-20 17:00に、画像生成プロンプトを汎用オフィス背景から、記事タイトル・元トピック・PDF内区分・ラベル・素材抜粋に基づくシーン分岐型へ変更した。採用面談、給与/社会保険実務デスク、契約更新面談、勤怠管理、安全衛生の工場/倉庫現場、社宅/住宅、メンタルヘルス相談、ハラスメント相談、年金/ライフプラン相談、ビジネス街などをテーマごとに選ぶ仕様にした。
- 2026-06-20 17:00に、本番オートメーション `ksrfp-weekly-run` の指示にも、毎記事ごとの新規画像生成、過去画像・同テーマ画像・汎用オフィス写真の再利用禁止、テーマ別構図分岐、`fresh_article_source` / `photo_source_fresh=true` の確認を追加した。
- 2026-06-20 17:00に、現在の3件で画像シーン分岐を確認した。1件目は採用面談、2件目は給与/社会保険実務デスク、3件目は契約更新・労働条件確認の面談に分岐する。`compileall` も成功。
- 2026-06-20 18:08に、既存下書き6件のアイキャッチ画像を新仕様で再生成・差し替えした。対象投稿IDは `4776`、`4778`、`4780`、`4782`、`4784`、`4786`。画像シーンは労働時間管理、社員研修、安全衛生現場、採用面談、給与計算、契約更新面談へ分岐し、写真ソースのハッシュ重複なし。WordPress再取得検証では全件 `draft` 維持、featured_media一致、画像形式OK、alt一致、メディアID/URL重複なしを確認した。検証結果は `04_wordpress/wordpress_featured_image_refresh_verify_latest.md`。
- 2026-06-20 18:18に、三回目の本番同等自動実行テスト `ksrfp-jinjiroumu-blog-final-test-3-1830` を18:30発火予定で設定した。既存6件は削除せず、本番同等の通常フローで3件新規生成し、手動補修なしで画像品質、Drive保存、WordPress下書き、通知、履歴を検証する。結果確認用フォローアップ `ksrfp-final-test-3-result-check` は19:20に設定した。本番の `ksrfp-weekly-run` は変更していない。
- 2026-06-20 19:25に、三回目の本番同等自動実行テストを不合格と判定した。18:30のテストは発火し、2026.5月号・2026.4月号を作成済みとしてスキップし、2026.3月号から3件を生成した。Google Drive確認用テキスト3件と、各記事ごとの新規写真品質アイキャッチ3件は作成できた。一方で、WordPress下書き保存は3件ともDNS/外向き通信エラーで失敗し、通知メールもSMTP/sendmailとも未送信だった。さらに、本文内で同一の定型段落が各記事20回以上繰り返されており、現行品質ゲートが同一記事内反復を見落としていた。したがって、この仕様をそのまま本番採用することはできない。
- 2026-06-20 20:08に、GitHub private repository `stwbstwb/geekom-codex-works` の `westhill-blog-automation` と `wordpress-article-automation` を確認した。成功実装では、外部連携プリフライト、投稿前停止ゲート、投稿後読み返し検証、通知once-marker、run単位状態管理が入っていることを確認し、比較結果を `00_project/成功実装比較メモ.md` に記録した。
- 2026-06-20 20:16に、成功実装の設計を反映した。`06_automation/run_external_preflight.py` と `src/ksrfp_jinjiroumu_blog/external_preflight.py` を追加し、Drive/WordPress/SMTPログインまでの本番同等プリフライトを実装した。`src/ksrfp_jinjiroumu_blog/quality_check.py` と `analyze_inputs.py` に同一記事内の段落反復ゲートを追加した。生成段階の確認用テキストDriveアップロードを止め、WordPress下書き保存・読み返し検証後にDrive保存する順序へ変更した。`src/ksrfp_jinjiroumu_blog/run_state.py` にrun単位状態管理を追加し、`run_wordpress_publish.py` で同じrunの二重投稿を抑止する。通知は `notification_sent.json` marker 方式へ変更した。
- 2026-06-20 20:16に、`run_external_preflight.py` を本番寄り条件で実行し、Drive PDF取得、WordPress REST API、カテゴリ取得、SMTP DNS/TCP、SMTPログインがすべてOKであることを確認した。`compileall src 06_automation` も成功した。本番オートメーション `ksrfp-weekly-run` の指示を、プリフライト必須、本文反復NG停止、WordPress検証後にDrive保存、最終通知1通の仕様へ更新した。
- 2026-06-20 20:19に、既存の問題記事3件へ新しい同一記事内反復ゲートを適用し、1件目21回、2件目20回、3件目21回の同一定型段落を検出できることを確認した。`run_wordpress_publish.py --all` のドライランも成功し、RunState連携後も3件のWordPressペイロードが `ready` になることを確認した。
- 2026-06-20 20:43に、三回目最終検証で作成された無効成果物を整理した。Google Drive確認用テキストは2026.3月号由来の3件を削除し、成功済みの2026.5月号・2026.4月号の6件のみ残した。WordPress下書き一覧をREST APIで直接取得し、プロジェクト由来の下書きは投稿ID `4776`、`4778`、`4780`、`4782`、`4784`、`4786` の6件のみで、表示上の残り3件は以前から存在する別件下書きと確認した。
- 2026-06-20 20:43に、`08_state/topic_history.json` から未投稿・未下書きの `ready_for_posting` / `generated` 履歴7件を削除し、重複防止対象を成功済み6件だけに戻した。`08_state/processed_pdfs.json` では2026.5月号・2026.4月号を `draft_saved_for_review` のまま保持し、2026.3月号は `failed_quality_gate` として未完了扱いにした。これにより次回通常フローでは2026.5月号・2026.4月号をスキップし、2026.3月号を再選定できる状態になった。
- 2026-06-20 20:43に、WordPress下書き一覧を取得する検証用スクリプト `06_automation/run_wordpress_draft_inventory.py` を追加した。`run_external_preflight.py` 再実行ではDrive、WordPress、SMTPがOKで、次回対象PDFは `人事労務だより2026.3月号.pdf` と確認した。`compileall src 06_automation` も成功した。
- 2026-06-20 20:45に、本番同等の最終クリーン再テスト `ksrfp-jinjiroumu-blog-final-clean-retest-2115` を21:15発火予定で設定した。既存6件は本番成果物として保持し、通常フローで2026.5月号・2026.4月号を作成済みとしてスキップし、未完了の2026.3月号を選び直すかを検証する。結果確認フォローアップ `ksrfp-final-clean-retest-result-check` も設定した。本番の `ksrfp-weekly-run` は変更していない。
- 2026-06-20 21:45に、21:15最終クリーン再テストを合格判定した。通常フローで2026.5月号・2026.4月号を作成済みとしてスキップし、2026.3月号から3件を作成。WordPress下書きIDは `4801`、`4803`、`4805`、Drive確認用テキストIDは `1o8NxPRG30SIl9txLG01ktAI372icGUBc`、`1tk3cbitsdRgzCjnG8Hj4lh5T1s1Z-CQD`、`1qsjSXLzVvKW2XMu3Ihw50ndKSq6Y9VXi`。本文反復なし、禁止表現なし、未確認ファクト0、写真背景ソース3件重複なし、WordPress一括検証OK、SMTP通知1通のみ送信を確認した。テスト用オートメーション `ksrfp-jinjiroumu-blog-final-clean-retest-2115` と結果確認 `ksrfp-final-clean-retest-result-check` は削除済み。本番の `ksrfp-weekly-run` は変更していない。
- 2026-06-20 22:21に、ユーザー指示により既存成果物9件を本番扱いせず削除対象へ変更した。WordPress下書き `4776`、`4778`、`4780`、`4782`、`4784`、`4786`、`4801`、`4803`、`4805` を削除し、Google Drive確認用テキスト9件も削除した。ローカル生成物と `processed_pdfs`、`topic_history`、`fact_check_registry`、`automation_status`、`scheduled_posts` を初期化した。
- 2026-06-20 22:21に、白紙状態を確認した。WordPress下書きは案件由来0件（別件の既存下書き3件のみ）、Google Drive確認用テキスト保存先は空、`03_generated/` 配下の生成物なし、`08_state/processed_pdfs.json` と `08_state/topic_history.json` は空。
- 2026-06-20 22:26に、記事作成履歴と一次情報確認ソースを分離した。白紙リセット後も法令・制度名の品質ゲートを維持できるよう、確認済み一次情報URLを `config/fact_check_verified_sources.json` に移し、`fact_check.py` でこの固定ソースと `08_state/fact_check_registry.json` を併用するよう変更した。記事化済みPDF・テーマ履歴は空のまま維持する。
- 2026-06-20 22:26に、白紙状態からローカル生成テストを実行し、2026.5月号から3件が自律選定されること、`fact_check_unverified=0`、`publication_gate=verified`、バッチ品質チェックOK、タイトル重複なし、タイトル型の偏りなし、構成型の偏りなしを確認した。ローカル生成後は再度リセットし、生成物・履歴を白紙へ戻した。
- 2026-06-21 11:40に、11:00 JST本番同等自動実行テストの結果を確認した。テスト本体は11:00に発火し、外部プリフライトではDrive PDF取得、WordPress REST API、カテゴリ、SMTPログインがOKだった。一方で、記事候補選定時に `analyze_inputs.py` の `read_json` import漏れにより `NameError: name 'read_json' is not defined` が3回連続で発生し、記事生成・画像生成・WordPress下書き・Drive保存へ進まなかった。失敗通知メールは11:02にSMTPで1通送信済み。テスト用オートメーション `ksrfp-jinjiroumu-blog-11-00-jst-automation-test` と結果確認 `ksrfp-11-00-jst-automation-test-result-check` は削除済み。
- 2026-06-21 11:45に、上記停止原因を修正した。`src/ksrfp_jinjiroumu_blog/analyze_inputs.py` の `io_utils` importへ `read_json` を追加し、`compileall src 06_automation` と `analyze_inputs` のimport確認が成功した。次回はこの停止点を越えるかを本番同等自動実行で再検証する。
