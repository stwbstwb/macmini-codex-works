# 開発ログ

## 2026-06-19

- プロジェクトフォルダを工程別に整理した。
- 入力データを `01_inputs/` に集約した。
- WordPressアプリケーションパスワード情報を `config/secrets/` に移動し、Git管理対象外にした。
- `06_automation/run_local_analysis.py` を追加した。
- `src/ksrfp_jinjiroumu_blog/` に解析基盤を追加した。
- GSC、GA、投稿済み記事、既存プロンプト、人事労務だよりPDFを初期解析できるようにした。
- PDFから記事候補トピックを抽出できるようにした。
- テーマ候補にSEO需要、実務影響、鮮度、カニバリリスクを加味するスコアリングを追加した。
- 強いカニバリリスクがあるテーマを避けて推奨テーマを選ぶ記事作成ブリーフを追加した。
- 記事作成ブリーフで、タイトル案、メタディスクリプション案、スラッグ案、一次情報確認チェックを出力できるようにした。
- 推奨テーマに応じた記事構成案を `03_generated/outlines/article_outline_latest.md` に出力できるようにした。
- 一次情報確認計画を `03_generated/outlines/source_check_plan_latest.md` に出力できるようにした。
- 記事本文ドラフトを `03_generated/articles/article_draft_latest.md` に出力できるようにした。
- 法律・制度・日付・数値を抽出し、`03_generated/articles/fact_check_items_latest.md` に根拠確認項目として出力できるようにした。
- ファクトチェック項目に、公的情報へ当たるための候補URLを出力できるようにした。
- 記事品質チェックを `03_generated/articles/article_quality_check_latest.md` に出力できるようにした。
- 未確認ファクトが残る場合は、下書き品質がOKでも公開可能判定を止めるようにした。
- `config/project_settings.json` を追加し、非秘密の運用設定を集約した。
- 処理済みPDF、テーマ履歴、確認済み根拠、直近実行状態の管理ファイルを `08_state/` に作成できるようにした。
- ファクトチェック確認済みレジストリを読み込み、確認済み項目を `verified` として反映できるようにした。
- WordPress投稿前ペイロードを `03_generated/wordpress-payloads/` に生成できるようにした。
- Markdown本文をWordPress向けHTMLへ変換できるようにした。
- 未確認ファクト、公開ゲート、WordPress接続テスト未完了がある場合、投稿ペイロードを `ready_to_send=false` にするようにした。
- アイキャッチ画像のサイズ、ファイル名、alt、生成プロンプトを `03_generated/images/featured_image_plan_latest.md` に出力できるようにした。
- Google Drive連携ステータスを `05_drive/drive_status_latest.md` に出力できるようにした。
- WordPress連携ステータスを `04_wordpress/wordpress_status_latest.md` に出力できるようにした。
- WordPressアプリケーションパスワード情報RTFから、値を出力せずにユーザー名・アプリケーションパスワードの検出可否を判定できるようにした。
- WordPressカテゴリ一覧、投稿者 `ksrfp`、タグなしルールを設定へ反映した。
- WordPress投稿ペイロードへ、記事テーマ・本文内容から判定したカテゴリID、投稿者ID、空タグを出力できるようにした。
- スラッグを設定しない投稿ルールを反映した。
- 当時の予約投稿日時を自動計算し、ローカル予約履歴と重複する場合は次回枠へ送る処理を追加した。
- Arkhe CSS Editorへ入力するCSSを設定と投稿ペイロードに保持できるようにした。
- 自動実行日を毎週月曜日0:00に設定した。
- 週次自動実行用の `06_automation/run_weekly_automation.py` を追加した。
- 自動実行時に最大3回リトライし、成功・失敗を問わず `stonewebstoneweb@gmail.com` へ通知する処理を追加した。
- SMTPまたはローカルsendmailで通知できない場合も、`.eml` バックアップと通知結果JSONを保存して処理を継続するようにした。
- Codexオートメーション `ksrfp-weekly-run` を作成し、毎週月曜日0:00にプロジェクトの週次ランナーを実行するよう設定した。
- `claude_works` 内の既存メール通知設定を確認し、SMTP秘密ファイル `config/secrets/email_smtp.json` を作成した。
- SMTP経由のテスト通知を実送信し、SMTPサーバーへの送信成功を確認した。
- 週次自動実行ラッパー全体を実行し、通知結果が `method=smtp` になることを確認した。
- SMTP秘密ファイル追加後にWordPress認証ファイル検出がSMTP設定を拾う問題を修正し、WordPressアプリケーションパスワードRTFを明示指定した。
- WordPress REST APIの読み取り専用認証確認を行い、ユーザーID `2` で接続できることを確認した。
- WordPressカテゴリ一覧を取得し、現行APIに合わせて `life-planning=13`、`subsidy=5` へカテゴリIDを補正した。
- WordPressタグ一覧と予約投稿一覧を読み取り専用で取得できるようにした。
- 公開Google DriveフォルダからPDF一覧を取得し、最新号 `人事労務だより2026.5月号.pdf` を `01_inputs/newsletters/drive-downloads/` に保存できるようにした。
- Drive取得PDFとローカルサンプルPDFの重複をUnicode正規化で除外するようにした。
- 外部連携の読み取り専用診断スクリプト `06_automation/run_external_diagnostics.py` を追加した。
- 処理済みPDFと生成テーマを `08_state/processed_pdfs.json`、`08_state/topic_history.json` に記録できるようにした。
- 予約投稿可能・投稿済みテーマを次回以降の重複回避対象にし、最新号に候補がない場合の過去号フォールバック候補を作れるようにした。
- 公的根拠が未確認のPDF内数値は本文へ直接引用しない方針に変更し、現在の記事の未確認ファクトを0件にした。
- アイキャッチ画像 `03_generated/images/working-hours-featured.png` を生成し、1200x630のPNGとして保存した。
- アイキャッチ画像計画で実ファイルを検出し、WordPressアップロード準備状態を判定できるようにした。
- WordPressメディアアップロードと下書き保存のガード付き関数を実装し、通常実行ではドライラン計画だけを生成するようにした。
- WordPress投稿ドライランスクリプト `06_automation/run_wordpress_publish.py` を追加した。
- 確認者向けテキストファイル生成を `src/ksrfp_jinjiroumu_blog/review_text.py` と `06_automation/run_review_text.py` に追加した。
- 確認用テキストのファイル名を `YYMMDD 記事タイトル.txt` 形式にし、`03_generated/review-texts/` へ保存するようにした。
- 確認用テキストのGoogle Drive保存先フォルダIDを設定へ追加し、Drive APIトークンがある場合はアップロード、ない場合は `auth_required` として処理を継続するようにした。
- 確認用テキストをGoogle Driveプラグイン経由で指定フォルダへ保存し、DriveファイルID `1dGP6x60T0bUUBNrYnDLRJ68a5m20Ke8O` を確認した。
- GAのページタイトル別表示回数を集計し、上位ページ・テーマ傾向・カテゴリ傾向を `02_analysis/seo/ga_content_insights.md` に出力できるようにした。
- 一次情報探索メモを `docs/一次情報探索メモ.md` に作成した。
- 実行ログを `07_logs/latest_run.json` と日時付きJSONに保存できるようにした。
- 初期解析結果を `02_analysis/` に出力した。

## 次の作業候補

- ファクトチェック項目に確認済みソースURL・確認日・根拠メモを保存できるようにする。
- 一次情報確認結果を記事本文へ自動反映する。
- テーマ別の記事本文テンプレートを拡充する。
- WordPress下書き保存までの自動実行テストを行う。
- 本番運用前リハーサルを行う。
